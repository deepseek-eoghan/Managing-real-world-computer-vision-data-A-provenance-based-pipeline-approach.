CREATE TABLE `configuration`.`camera_app_config` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `camera_id` INT NOT NULL,
  `created` DATETIME NOT NULL,
  `corner_mask_X1` DOUBLE NOT NULL,
  `corner_mask_Y1` DOUBLE NOT NULL,
  `corner_mask_X2` DOUBLE NOT NULL,
  `corner_mask_Y2` DOUBLE NOT NULL,
  `corner_mask_X3` DOUBLE NOT NULL,
  `corner_mask_Y3` DOUBLE NOT NULL,
  `corner_mask_X4` DOUBLE NOT NULL,
  `corner_mask_Y4` DOUBLE NOT NULL,
  `min_points_in_path` INT NOT NULL,
  `write_paths_to_dynamo` BOOLEAN NOT NULL,
  `round_places` INT NOT NULL,
  `max_snake_len` INT NOT NULL,
  `heatmap_columns` INT NOT NULL,
  `count_line_X1` DOUBLE NOT NULL,
  `count_line_X2` DOUBLE NOT NULL,
  `count_line_Y1` DOUBLE NOT NULL,
  `count_line_Y2` DOUBLE NOT NULL,
  `transformation_shape_width` INT NOT NULL,
  `transformation_shape_height` INT NOT NULL,
  `camera_roi_length` INT NOT NULL,
  `roi_ground_coverage` DOUBLE NOT NULL,
  `speed_divider` DOUBLE NOT NULL,
  `social_distance_guideline` DOUBLE NOT NULL,
  `clear_paths_frame_interval` INT NOT NULL,
  PRIMARY KEY (`id`));
